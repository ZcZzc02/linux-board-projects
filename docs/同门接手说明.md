# K7 项目接手说明

这份文件可以直接发给接手人，也可以直接放到他电脑上的项目根目录后让 Codex 阅读。

## 一句话目标

维护 KICKPI K7 / RK3576 Linux 网关，使 STM32F767 节点板的 LoRa 上行数据（AD7606、CAN、RS485）经 K7 发送到 HiveMQ，并支持 Windows 上位机显示和后台下发到节点板。

## 交接时你只需要收到两样东西

1. 本文件：`同门接手说明.md`。
2. `K7交接包-源码与关键备份-可直接解压.zip`。

正常接手同一块 K7 板子时，不需要先索取或刷写官方系统镜像。ZIP 已包含继续开发所需的源码、关键 UART3 备份、烧录工具和资料。官方全量镜像仅在板子无法启动、确认需要恢复系统时，再向原交接人单独索取。

## 收到后按此操作

1. 在任意目录克隆下面的 GitHub 分支。
2. 将 ZIP **直接解压到 clone 后的项目根目录**，选择“合并/覆盖目录”。ZIP 内的 `1.网关`、`2.节点F767`、`backups` 会落到正确位置。
3. 用 VS Code 打开项目根目录，让 Codex 先阅读 `AGENTS.md`、本文件、`docs/K7项目交接指南.md` 和 `docs/INTERFACE_SPEC.md`。
4. 不要先刷机。先连接 K7 的 ADB，按“`K7 接手验证顺序`”验证现有系统、UART3、LoRa、WiFi 和 HiveMQ。
5. 只有 GitHub 不可访问时，才使用 ZIP 内的 `linux-board-projects-handover.bundle` 离线克隆。

## 当前 Git 分支

```text
仓库：https://github.com/ZcZzc02/linux-board-projects.git
分支：feat/k7-gateway-service-downlink
```

克隆后执行 `git log -1 --oneline`，以该分支的最新提交为准。

当前 K7 和上位机统一使用：

```text
MQTT Broker: broker.hivemq.com:1883
Topic prefix: fengyan_daq_2026
K7 LoRa device: /dev/ttyS3, 9600 8N1
```

物联网卡的定向流量只允许 `broker.hivemq.com`。不要把当前代码、服务或上位机改为 `broker.emqx.io`，不同 Broker 的 topic 不会互通。

## 官方镜像（可选，不是本次必须发送）

官方镜像文件名必须是：

```text
update-rk3576-kickpi-k7-linux-ubuntu-multi-20251209-191847.img
```

该文件约 4.2 GB，没有放进 ZIP。只有板子无法启动且确认需要恢复官方系统时，再单独索取；没有必要时不要刷它。全量刷机后会丢失当前 UART3 的 boot/resource 配置。

## 新电脑第一步

若能访问 GitHub：

```powershell
git clone --branch feat/k7-gateway-service-downlink --single-branch https://github.com/ZcZzc02/linux-board-projects.git
cd linux-board-projects
git log -1 --oneline
```

应看到：

```text
c34cac5 fix: restore HiveMQ broker configuration
```

将“可直接解压”版本的 ZIP 解压到项目根目录。ZIP 中的目录名已经与项目一致，例如 `1.网关`、`2.节点F767`、`backups`；解压时选择“覆盖/合并目录”，不要再多套一层同名文件夹。

若不能访问 GitHub，先从 ZIP 中的 `linux-board-projects-handover.bundle` 克隆：

```powershell
git clone .\linux-board-projects-handover.bundle linux-board-projects
cd linux-board-projects
git switch feat/k7-gateway-service-downlink
```

## 当前硬件和服务事实

- 板卡：KICKPI K7，RK3576，Ubuntu 24.04。
- LoRa 模块：E22-400T22D，连接 K7 UART3，Linux 设备名 `/dev/ttyS3`。
- 设备树目标：UART3 为 `okay`，I2C7 为 `disabled`。
- 节点板：STM32F767；Keil 工程与当前已编译 `工程文件.hex` 在交接包中。
- 5G 模块：Quectel RG200U-CN。无 SIM 卡或模块接触不良时，网络守护会回退 WiFi。
- 当前 K7 服务：`k7-gateway.service` 和 `k7-network-watchdog.service`，均已设置开机启动。
- WiFi 已验证可发布 HiveMQ 状态：`{"online":1,"transport":"wifi"}`。

RG200U 必须用合适的短 M2 螺丝固定。未固定时曾出现 USB 设备与 `enx*` 网卡反复消失；这不是软件或 Broker 能解决的问题。

## 绝对不要先做的事

1. 不要因为 LoRa、5G 或上位机问题就先重刷 K7。
2. 不要把官方全量镜像当作当前 UART3 版本的备份。
3. 不要把 `boot-resource-uart3.img` 当作 FactoryTool 的全量 `update.img`。
4. 不要手写 Rockchip 分区扇区偏移执行写入命令。
5. 不要提交或传播 WiFi 密码、SIM 卡信息、token、私钥、历史数据库。

## K7 接手验证顺序

先让 K7 正常上电，接上调试 USB。在 Windows 终端执行：

```powershell
adb devices
adb shell "systemctl is-active k7-gateway.service k7-network-watchdog.service"
adb shell "cat /proc/device-tree/serial@2ad60000/status; echo; cat /proc/device-tree/i2c@2aca0000/status; echo; ls -l /dev/ttyS3"
adb shell "nmcli device status; ip route"
```

期望：

```text
两个服务均为 active
UART3: okay
I2C7: disabled
/dev/ttyS3 存在
WiFi 或蜂窝网络存在 default route
```

LoRa 检查：

```powershell
adb shell "cd /root/k7-gateway && PYTHONPATH=src python3 -m k7_gateway listen-lora --device /dev/ttyS3 --seconds 30 --raw"
```

节点板上电并发送时，应该看到 `RX_HEX=` 和解析后的 JSON。详细协议和完整验收请读：

- `docs/K7项目交接指南.md`
- `docs/INTERFACE_SPEC.md`
- `2.1.Linux网关/k7-gateway/docs/network-watchdog.md`

## Windows 上位机启动

```powershell
cd .\5.网络上位机服务\server
py -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
.\启动上位机-浏览器模式.bat
```

浏览器打开 `http://127.0.0.1:8000`。页面应显示 `HiveMQ 已连接` 和 `WebSocket 已连接`。K7 在线后状态应显示 `WiFi`、`4G` 或 `5G`，由 K7 默认路由决定。

## 给 Codex 的工作约束

在这个项目中，每次先执行：

```powershell
git status --short --branch
```

然后先读 `AGENTS.md`、本文件和 `docs/K7项目交接指南.md`。每次只完成一个可验证目标；修改多个文件或系统配置前创建分支；运行测试、检查 `git diff` 后再 commit。不要自动刷机、改分区、执行 `dd`、`rm -rf` 或删除备份。

## ZIP 内应有的内容

```text
linux-board-projects-handover.bundle
1.网关/onenet_v1/                         # 旧 STM32 网关，仅作协议对照
2.节点F767/Onenet_BigBoard (2)_V4/        # F767 Keil 源码
2.节点F767/.../工程文件.hex                # 当前已编译节点固件
backups/k7-resource-uart3-candidate-20260627/
backups/k7-official-baseline-20260627-131743/
work/factorytool-original/                # FactoryTool v1.89
work/boot-uart3.its
work/sdk-dts-backup-20260627/
KICKPI-K7规格书.pdf
K7当前状态交接与续开发指南.md
docs/同门接手说明.md
```

ZIP 不包含：官方 4.2 GB Ubuntu 全量镜像、整个 K7 SDK、整个 RK3576 厂家资料目录、Python `.venv`、SQLite 数据库、日志、Keil Objects 编译缓存。这些文件要么体积过大，要么可在新电脑重建，要么不适合传播。
