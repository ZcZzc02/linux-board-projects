# K7 当前状态交接与续开发指南

> 历史状态说明：本文前半部分记录了 UART3/E22 刚打通时的硬件排查过程，保留它是为了追溯踩坑，不代表当前项目仍停留在那个阶段。当前实际完成状态以 `docs/同门接手说明.md`、`docs/K7项目交接指南.md` 和 Git 分支 `feat/k7-gateway-service-downlink` 为准。

## 1. 文档用途

这份文档不是“重新买一套 K7 从零开始”的入门说明，而是给另一台电脑上的开发者或 VS Code 的 Codex 插件做交接用的。

默认前提：

- 继续使用当前这块已经调试过的 `K7-RK3576`
- 继续使用当前这套已经接通过的硬件
- 新电脑上的目录名、盘符、用户名都可能不同
- 但关键文件名和目录结构保持不变
- 目标是接着当前板子状态继续开发

这份文档的作用：

- 让新的 Codex 快速同步到当前真实状态
- 让新的开发者不要再把已经打通的部分从头折腾一遍

## 2. 路径约定

后文统一使用 3 个占位符，不再绑定某一台电脑的绝对路径：

- `<workspace_root>`：当前项目仓库根目录
- `<k7_project_root>`：`<workspace_root>/2.1.Linux网关/k7-gateway`
- `<k7_aux_root>`：K7 额外资料工作目录，里面放 `1-SDK`、`patches`、`artifacts`、`backup`

### 2.1 如何判断 `<workspace_root>`

`<workspace_root>` 下通常应能看到这些内容：

- `AGENTS.md`
- `项目概览.md`
- `docs/INTERFACE_SPEC.md`
- `2.1.Linux网关/k7-gateway`

### 2.2 如何判断 `<k7_project_root>`

`<k7_project_root>` 下应至少能看到：

- `README.md`
- `docs`
- `src`
- `tests`
- `tools`
- `pyproject.toml`

### 2.3 如何判断 `<k7_aux_root>`

`<k7_aux_root>` 下应至少能看到：

- `1-SDK`
- `patches`
- `artifacts`
- `backup`

### 2.4 如果新的 Codex 找不到路径

新的 Codex 不要先猜盘符和用户名，优先按文件名搜索：

- `K7当前状态交接与续开发指南.md`
- `INTERFACE_SPEC.md`
- `LoRa_E22接线与UART选择.md`
- `enable-uart3-40pin.patch`
- `mmcblk2p3-full-20260623.itb`

## 3. 给新的 Codex 的第一条指令

如果你是新电脑上的 Codex，请先按这个顺序读文件，不要一上来就改代码：

1. 当前文件
   `<k7_project_root>/docs/K7当前状态交接与续开发指南.md`
2. 接口总约定
   `<workspace_root>/docs/INTERFACE_SPEC.md`
3. K7 的 E22 接线文档
   `<k7_project_root>/docs/LoRa_E22接线与UART选择.md`
4. K7 工程说明
   `<k7_project_root>/README.md`

然后遵守下面这些限制：

- 当前优先处理对象是 `K7 Linux 网关板`
- 先不要改节点板、STM32 网关板和上位机
- 先以串口控制台为主，不要默认 GUI、屏幕、SSH 一定正常
- 先验证当前板子状态，再决定是否需要重刷、重编译、改设备树

## 4. 当前项目与关键文件位置

### 4.1 Git 分支

当前开发分支：

`feature/k7-gateway`

注意：

- 当前整个项目仓库不是纯净工作区
- 里面有 STM32 网关、上位机、资料文件等其他改动
- 新的 Codex 不要因为要调 K7，就误改别的子系统

### 4.2 K7 工程目录

K7 Linux 网关工程目录：

`<k7_project_root>`

K7 文档目录：

`<k7_project_root>/docs`

K7 Python 代码入口：

`<k7_project_root>/src/k7_gateway/main.py`

### 4.3 SDK / 补丁 / 产物 / 备份

K7 SDK 工作目录：

`<k7_aux_root>/1-SDK/Armbian/kickpi-armbian-20250917/kickpi-armbian`

UART3 相关补丁：

`<k7_aux_root>/patches/enable-uart3-40pin.patch`

UART3 相关产物：

- `<k7_aux_root>/artifacts/boot-uart3.img`
- `<k7_aux_root>/artifacts/zboot-uart3.img`
- `<k7_aux_root>/artifacts/update-rk3576-kickpi-k7-linux-ubuntu-20260625-173050.img`

备份文件：

`<k7_aux_root>/backup/mmcblk2p3-full-20260623.itb`

### 4.4 已确认修改过的 SDK 源文件

UART3 40Pin 相关设备树源文件：

`patch/kernel/rk35xx-vendor-6.1/dt/rk3576-kickpi-k7-extend-40pin.dtsi`

说明：

- 这是源码设备树里的 40Pin 扩展定义
- `enable-uart3-40pin.patch` 本质上就是对这个文件修改后的差异保存
- `artifacts` 里的文件是这条修改链路上生成过的结果

## 5. 当前 K7 板子的真实状态

这部分最重要。新的接手人必须先相信“当前真实状态”，不要再拿早期失败记录做判断。

### 5.1 当前已经确认成功的事实

当前 K7 已确认：

- 系统能从串口控制台进入 shell
- `/dev/ttyS3` 已存在
- `UART3` 在运行时设备树中是 `okay`
- `I2C7` 在运行时设备树中是 `disabled`
- `dmesg` 已能看到 `ttyS3 at MMIO 0x2ad60000`
- K7 本地 `UART3` 回环已打通
- K7 通过 `UART3` 已成功读到 E22 模块响应

### 5.2 已验证过的运行时状态

#### 运行时设备树状态

执行：

```bash
printf 'uart3 status: ['; tr -d '\0' < /sys/firmware/devicetree/base/serial@2ad60000/status; printf ']\n'
printf 'i2c7 status: ['; tr -d '\0' < /sys/firmware/devicetree/base/i2c@2aca0000/status; printf ']\n'
```

期望结果：

```text
uart3 status: [okay]
i2c7 status: [disabled]
```

#### 当前可见串口

执行：

```bash
ls -l /dev/ttyS*
```

实际已经见到过：

```text
/dev/ttyS3
/dev/ttyS4
/dev/ttyS6
/dev/ttyS8
```

#### dmesg 中 UART3 的识别结果

执行：

```bash
dmesg | grep -i -E "ttyS3|2ad60000|uart3"
```

关键结果：

```text
2ad60000.serial: ttyS3 at MMIO 0x2ad60000 (irq = 47, base_baud = 11718)
```

同时还出现过一条警告：

```text
dw-apb-uart 2ad60000.serial: failed to request DMA, use interrupt mode
```

当前判断：

- 这是警告，不是当前阻塞项
- 串口仍然可以工作，系统会退回中断模式

### 5.3 已通过的 K7 UART3 本地回环

我们已经验证过：

- 把 K7 40Pin 的 `22` 脚和 `24` 脚短接
- 用 `UART3` 发送字符串
- 能在本机读回

成功读回过的内容：

```text
U3TEST1234
```

这说明：

- K7 自己的 UART3 软件层是通的
- UART3 的 40Pin 复用已经生效

## 6. 当前 LoRa E22 模块状态

### 6.1 模块型号

当前使用模块：

`EBYTE E22-400T22D`

### 6.2 当前这块板子上，E22 不要再随便改线

这里一定注意：

- 前面踩过一次“理论接法正确，但实物线序不通”的坑
- 最终是保留当前成功接法后，K7 才成功读回 E22 响应
- 所以新的接手人不要只看纸面 RX/TX 理论就去“改回去”

如果必须重接线，原则是：

- 以当前这块已通板子的现有接法为准
- 改完必须再跑一遍下面的 E22 读配置测试

### 6.3 当前已确认的 E22 电平

实测电压曾见到：

- `VCC-GND` 约 `5.2V`
- `M0-GND` 约 `3.2V`
- `M1-GND` 约 `3.2V`
- `AUX-GND` 约 `3.2V`
- `TXD-GND` 约 `3.2V`

### 6.4 当前已成功的 E22 配置读取结果

当前板子上已经成功执行过 E22 读配置指令，结果为：

```text
TX_HEX= c10009
RX_LEN= 12
RX_HEX= c100090000006500170b0000
```

这条结果证明：

- `K7 UART3` 是通的
- `E22` 已经有响应
- 当前串口方向和模块模式至少在“读配置”这件事上是正确的

### 6.5 做这次成功读取时的模式

做 E22 读配置时，用的是配置模式：

- `M0 -> GND`
- `M1 -> 3.3V`

注意：

- 这是为了读配置寄存器
- 不是 LoRa 透传收发的最终工作模式

## 7. 当前完成状态与真实剩余事项

本节已根据当前项目状态更新。原来“还没完成”的 5 项已经完成并部署到 K7：

1. `k7_gateway` 已正式使用 `/dev/ttyS3`，systemd 的 `k7-gateway.service` 也固定传入 `--device /dev/ttyS3`。
2. E22 已从读配置验证进入透明传输工作流；节点板开机时，K7 可接收真实 LoRa 数据。
3. K7 已实现 LoRa 帧缓存、CRC-16/MODBUS 校验和 TYPE 解包，支持 AD7606（`0x01`）、CAN（`0x02`）、RS485（`0x03`）及 RS485 透传（`0x41`）。
4. K7 已将解析后的数据发布到 `broker.hivemq.com:1883`，topic 前缀为 `fengyan_daq_2026`；已验证 WiFi 上行和 Windows 上位机链路。
5. `k7-network-watchdog.service` 已实现蜂窝网络优先、已保存 WiFi 兜底、蜂窝恢复后重新优先的网络状态机。
6. 后台下发已支持 `rs485_raw`、`rs485_read`、`rs485_write`、CAN relay 和 RS485 relay，并由 K7 转发到 LoRa 节点。

当前 K7 的完整业务链路已经具备可用状态：

```text
F767 节点板 -> LoRa E22 -> K7 UART3 -> HiveMQ -> Windows 上位机
Windows 上位机 -> HiveMQ -> K7 -> LoRa E22 -> F767 节点板
```

### 7.1 仍建议继续验证的事项

这些不是“功能未实现”，而是需要更长时间或真实现场设备复验的风险项：

- RG200U-CN 必须用 M2 螺丝固定。未固定时曾发生 USB/`enx*` 网卡掉线；软件无法解决接触不良。
- 5G 链路需要在模块固定、插入有效 SIM 卡后做长时间稳定性测试。当前无 SIM 卡时，系统应按设计回退到 WiFi。
- RS485 协议和下发代码已实现，但仍建议接真实 RS485 从机做读、写和透传闭环验收。
- UART3 boot/resource 当前有已验证的二进制备份；后续可用 K7 SDK 重建设备树和 boot，以获得更完整的可重复构建流程。

### 7.2 当前关键代码位置

- K7 网关：`<k7_project_root>/src/k7_gateway/`
- K7 systemd 服务：`<k7_project_root>/systemd/`
- 网络守护：`<k7_project_root>/src/k7_gateway/network_watchdog.py`
- 协议/CRC：`<k7_project_root>/src/k7_gateway/lora_protocol.py`
- 后台下发：`<k7_project_root>/src/k7_gateway/downlink.py`

## 8. 新电脑接手时，最少需要准备什么

如果只是接着当前板子继续开发，不需要把之前所有步骤从头重做。

### 8.1 必备软件

- `VS Code`
- `Remote - SSH`
- `MobaXterm` 或其他串口终端
- `Git`

### 8.2 可选软件

只有在需要重新编译 K7 镜像、设备树或内核时，才需要：

- `WSL Ubuntu 24.04`

说明：

- 如果只是继续写 K7 Python 网关应用，不一定马上需要 WSL
- 如果要再改 UART3 相关镜像、设备树、boot 镜像，就需要 WSL + SDK

## 9. 新电脑接手时，建议的实际顺序

### 第一步：先找到工作区根目录

先找到 `<workspace_root>`。

### 第二步：先让新的 Codex 读本文档

不要上来就让它扫全仓库。

先给它看：

- 当前文件
- `<workspace_root>/docs/INTERFACE_SPEC.md`
- `<k7_project_root>/docs/LoRa_E22接线与UART选择.md`
- `<k7_project_root>/README.md`

### 第三步：先连串口，不先赌 SSH

当前最稳妥的接手方式：

1. 用串口控制台进 K7
2. 在 K7 上先确认 `/dev/ttyS3`
3. 再看 SSH 和网络是否可用

原因：

- 前面多次碰到过 GUI、黑屏、网络、维护模式等问题
- 串口控制台一直是最可靠的恢复入口

### 第四步：先做 30 秒自检

先执行下面四条：

```bash
printf 'uart3 status: ['; tr -d '\0' < /sys/firmware/devicetree/base/serial@2ad60000/status; printf ']\n'
printf 'i2c7 status: ['; tr -d '\0' < /sys/firmware/devicetree/base/i2c@2aca0000/status; printf ']\n'
ls -l /dev/ttyS*
dmesg | grep -i -E "ttyS3|2ad60000|uart3"
```

如果这四条都对，说明你已经接到了我们现在这条开发线上。

### 第五步：如有必要，再复核 E22

如果你怀疑线被动过，或者别人重新插过 E22，再跑一次配置读取验证。

先设串口：

```bash
stty -F /dev/ttyS3 9600 cs8 -cstopb -parenb -ixon -ixoff raw
```

再执行：

```bash
python3 - <<'PY'
import os, termios, time, select, binascii

cmd = bytes.fromhex('C1 00 09')
fd = os.open('/dev/ttyS3', os.O_RDWR | os.O_NOCTTY)

a = termios.tcgetattr(fd)
a[0] = 0
a[1] = 0
a[2] = ((a[2] & ~termios.PARENB & ~termios.CSTOPB & ~termios.CSIZE) |
        termios.CS8 | termios.CLOCAL | termios.CREAD)
a[3] = 0
a[4] = termios.B9600
a[5] = termios.B9600
termios.tcsetattr(fd, termios.TCSANOW, a)
termios.tcflush(fd, termios.TCIOFLUSH)

os.write(fd, cmd)
time.sleep(0.2)

buf = b''
end = time.time() + 3
while time.time() < end:
    r, _, _ = select.select([fd], [], [], 0.2)
    if r:
        try:
            buf += os.read(fd, 256)
        except Exception:
            pass

print('TX_HEX=', binascii.hexlify(cmd).decode())
print('RX_LEN=', len(buf))
print('RX_HEX=', binascii.hexlify(buf).decode())
os.close(fd)
PY
```

期望关键结果：

```text
RX_LEN= 12
RX_HEX= c100090000006500170b0000
```

## 10. 当前不要重复做的事情

如果只是继续开发，请不要先做这些动作：

- 不要先重刷系统
- 不要先手工改板子里的 DTB
- 不要先把 E22 线全部拔掉重接
- 不要先把项目代码从 `ttyS3` 改回别的串口
- 不要先去处理屏幕横竖屏问题

原因很简单：

- 当前真正已经通的是 `UART3 + E22 读配置`
- 这条链路是现在最宝贵的“已验证基线”

## 11. 推荐下一步开发路线

新的接手人建议按下面顺序推进：

1. 把 `<k7_project_root>/src/k7_gateway/main.py` 的串口候选切到 `/dev/ttyS3`
2. 把 E22 从“读配置”阶段推进到“透传收发”阶段
3. 在 K7 上先打通 LoRa 原始帧接收和十六进制日志
4. 按 `<workspace_root>/docs/INTERFACE_SPEC.md` 实现 LoRa 帧解析、CRC、TYPE 拆包
5. 再接 MQTT 上报
6. 最后再补网络优先级、4G/5G、守护和异常恢复

### 11.1 优先级建议

当前最合理的优先级是：

`UART3/E22 已通基线`
-> `LoRa 透明接收`
-> `协议解包`
-> `MQTT 上报`
-> `网络状态机`
-> `界面/屏幕/体验问题`

## 12. 回退与恢复

如果后续又把板子折腾坏了，优先按这个顺序恢复：

### 12.1 最稳妥恢复路径

1. 先保留当前串口日志
2. 优先用串口控制台判断是系统问题还是网络问题
3. 需要整块恢复时，优先考虑：
   - 匹配屏幕版本的原厂镜像
   - 或现有备份 `mmcblk2p3-full-20260623.itb`

### 12.2 现有恢复文件

备份：

`<k7_aux_root>/backup/mmcblk2p3-full-20260623.itb`

说明：

- 这是已有备份文件
- 恢复前仍要确认你恢复的是哪部分分区、对应哪套系统状态

### 12.3 关于 artifacts

下列文件存在于 `<k7_aux_root>/artifacts`：

- `boot-uart3.img`
- `zboot-uart3.img`
- `update-rk3576-kickpi-k7-linux-ubuntu-20260625-173050.img`

注意：

- 这些文件是 UART3 调试过程中生成过的产物
- 不建议新的接手人一上来就直接刷
- 除非他明确知道自己在接续“镜像重编译和重刷”这条线

## 13. 给下一位开发者的最短交接结论

如果只看一段话，请看这一段：

> 当前这块 K7 已经不是原始状态。现在真实基线是：`UART3 已启用`、`I2C7 已关闭`、`/dev/ttyS3 已存在`、`K7 本地 UART3 回环已通过`、`E22-400T22D 已通过 UART3 返回配置响应 c100090000006500170b0000`。下一步不要再从“怎么开 UART3”重新折腾，而是应该直接在 `<k7_project_root>` 里把应用层串口切到 `/dev/ttyS3`，继续做 LoRa 透传收包、协议解析和 MQTT 上报。
